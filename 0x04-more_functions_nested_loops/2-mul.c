#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first number to be checked
 * @b: second number to be checked
 * Return: the results of the two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
